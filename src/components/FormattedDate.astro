---
interface Props {
  date: Date;
}

const { date } = Astro.props;

const formatted = new Intl.DateTimeFormat('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
}).format(date);
---

<time datetime={date.toISOString()} class="text-gray-500 text-sm">
  {formatted}
</time>
